import React from 'react';
import './Navbar.css'; // (optional if you have navbar-specific CSS)

const Navbar = () => {
  return (
    <header className="site-header">
      <div className="wrapper">
        <h1 className="site-title">Past Life Regression Therapy</h1>
        <p className="site-subtitle">with Soul’s Purpose</p>
      </div>
    </header>
  );
};

export default Navbar;
